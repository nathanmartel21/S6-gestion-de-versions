## Commands :

```
cd atelier31/
ls
cd the-art-of-command-line/
git remote
git remote -v
git log --oneline --graph --all | head
git log --oneline | head -n 2
git commit --amend -m "Conjugaison correcte du verbe aimer."
git log --oneline | head -n 2
git push
git pull
git push
git pull --rebase
git status
git log --oneline --graph --all | head -n 6
```

## Exercice :

```
cd ~/formation-git/atelier32
ls
git remote -v
git log --oneline --graph --all
git commit --amend -m "Changement du message du dernier commit et aussi de son nom de commit (3ae5ar)
git push
git pull || git pull --rebase #permet de réconcilier deux branches divergentes.
git push
git status
```
