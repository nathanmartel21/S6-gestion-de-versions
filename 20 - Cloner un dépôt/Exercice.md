## Commands :

```
mkdir -p atelier31
cd atelier31/
git clone https://github.com/jlevy/the-art-of-command-line.git
ls
cd the-art-of-command-line/
ls
git log --oneline --graph --all
cd ..
rm -rf the-art-of-command-line/
git clone git@github.com:nathanmartel21/the-art-of-command-line.git
cd the-art-of-command-line/
git log --oneline --graph --all
```


## Exercice : 

```
mkdir -p atelier32
cd atelier32/
git clone git@gitlab.com:nathanmartel21/the-art-of-command-line.git
ls
cd the-art-of-command-line/
ls
git log --oneline --graph --all
```
